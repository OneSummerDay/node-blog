<div class="author">
    <h1 class="author__heading">
        Hi, I'm Artem
    </h1>
    <p class="author__body">
        Beginner WEB developer
    </p>
</div>

<img src="/img/hero-image.webp" alt="person looking through the window" class="hero-image" width="981" height="528">

<section class="articles">
    <h2 class="articles__heading">Latest Posts</h2>

    <ul class="article-ul">
        <% data.forEach(post => { %>       
            <li>
                <a href="/post/<%= post._id %>">
                    <span><%= post.title %></span>
                    <span class="article-list__date"><%= post.createdAt.toDateString() %></spa>
                </a>
            </li>
         <% }) %>
    </ul>

    <a href="#" class="pagination">&lt; View Older Posts</a>
</section>